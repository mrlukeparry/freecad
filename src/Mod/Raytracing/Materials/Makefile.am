# Change data dir from default $(datadir) to $(datadir)/Mod/Robot/Lib
datadir = @datadir@/Mod/Raytracing/Materials

# For Debian based system we mustn't pick up these files
if MAKE_NO_DFSG_PACKAGE

data_DATA = \


EXTRA_DIST = \
		$(data_DATA) \
endif

